\section{GGPKD: Global Geometry through Local Alignment}
\label{sec:method}

GGPKD approximates corpus-level teacher geometry through three operations. A teacher graph selects \emph{relevant} local relations, an ambient target \emph{calibrates} omitted graph relations over the scored pool, and a shared candidate pool \emph{amortizes} both anchor-centered and auxiliary-row supervision. All teacher-side quantities are cached before optimization.

\subsection{From Global Geometry to Local Alignment}
\label{sec:relation-selection}

Let $\mathcal D=\{x_i\}_{i=1}^{N}$ be an unlabeled corpus, and let $t_i$ and $s_i$ be the unit-normalized teacher and student embeddings. For each anchor $i$, let $p_i^T$ be a teacher relation distribution over $\mathcal D\setminus\{x_i\}$. With teacher and student cosine similarities $c_{ij}^T$ and $c_{ij}^S$, define the bounded pair distortion
\begin{equation}
\ell_{ij}=\frac{(c_{ij}^S-c_{ij}^T)^2}{4}\in[0,1].
\end{equation}
The quantity
\begin{equation}
\mathcal E
=\frac1N\sum_i\sum_{j\neq i}p_i^T(j)\ell_{ij}
\label{eq:global-distortion}
\end{equation}
formalizes the global teacher-weighted geometry we ultimately seek to preserve; it is not an objective that we compute directly.

Conventional relational KD instead observes the examples that happen to share a mini-batch. The following proposition makes the resulting mismatch precise; Appendix~\ref{app:batch-proof} gives the proof.

\begin{proposition}[Mini-batch exposure is independent of teacher relevance]
\label{prop:batch-exposure}
Let $\mathcal O_i^{(T)}$ be the examples co-occurring with anchor $i$ over $T$ independent random reshuffles into batches of size $B$. For any teacher relation distribution $p_i^T$,
\begin{equation}
\mathbb E\!\left[1-p_i^T(\mathcal O_i^{(T)})\right]
=\left(1-\frac{B-1}{N-1}\right)^T.
\label{eq:batch-exposure}
\end{equation}
\end{proposition}

Thus important and irrelevant relations have the same inclusion probability. This suggests replacing arbitrary batch-local geometry with teacher-defined local geometry. The next proposition explains when a collection of such local objectives is sufficient; its proof is in Appendix~\ref{app:local-global-proof}.

\begin{proposition}[Local-to-global geometry bound]
\label{prop:global-geometry}
For any selected relation set $C_i$ with positive teacher mass, define
\begin{equation}
\epsilon_i
=\mathbb E_{j\sim p_i^T(\cdot\mid C_i)}[\ell_{ij}],
\qquad
\delta_i=1-p_i^T(C_i),
\end{equation}
and let $\bar\epsilon=N^{-1}\sum_i\epsilon_i$ and $\bar\delta=N^{-1}\sum_i\delta_i$. Then
\begin{equation}
\mathcal E\leq\bar\epsilon+\bar\delta.
\label{eq:global-geometry-bound}
\end{equation}
\end{proposition}

Global preservation therefore reduces to two requirements: accurately align the selected local geometry and cover most teacher-relevant mass. Appendix~\ref{app:proportional-bound} bounds the missed mass under teacher-proportional sampling, and Appendix~\ref{app:concentrated-support} contrasts its exposure rate with random batches.

\subsection{Relevance: Teacher-Defined Local Geometry}
\label{sec:graph}

We cache the teacher embeddings and form the mutual $k$-nearest-neighbor set
\begin{equation}
\mathcal M_k^T(i)
=\operatorname{kNN}_T(i)
\cap\{j:i\in\operatorname{kNN}_T(j)\}.
\end{equation}
Retained edges are weighted by teacher cosine similarity and row-normalized:
\begin{equation}
P^T_{ij}
=\frac{\mathbf 1[j\in\mathcal M_k^T(i)]\exp(c^T_{ij}/\tau_i)}
{\sum_{\ell\in\mathcal M_k^T(i)}\exp(c^T_{i\ell}/\tau_i)}.
\label{eq:teacher-transition}
\end{equation}
The row $P_i^T$ defines the immediate local geometry around anchor $i$. Mutuality suppresses one-directional hub edges while preserving relations endorsed from both endpoints. We choose $\tau_i$ by bisection to satisfy $H(P_i^T)=\log\rho$ whenever the row degree permits, where $\rho$ is a shared target perplexity. Lower-degree rows use their maximum feasible entropy, and an isolated mutual-neighbor row falls back to the teacher's nearest neighbor. This entropy calibration gives every non-degenerate row a comparable effective neighborhood size while adapting its bandwidth to local density.

\subsection{Optional Radius Extension}

The reference method uses the one-hop transition target $g^T_{i,1}=P_i^T$. To test whether broader graph propagation is necessary, we also define an optional radius extension with the lazy operator $\widetilde P^T=\tfrac12(I+P^T)$:
\begin{equation}
g^T_{i,1}=P_i^T,
\qquad
g^T_{i,r}
=\operatorname{Norm}_{\mathcal D\setminus\{i\}}
\!\left(e_i^\top(\widetilde P^T)^r\right),
\quad r>1.
\label{eq:diffusion-targets}
\end{equation}
Here $\operatorname{Norm}_A$ restricts a nonnegative vector to $A$ and renormalizes it. The lazy walk prevents periodic oscillation, while $r>1$ describes progressively broader teacher neighborhoods without constructing a student graph. To keep these optional targets sparse, after each multiplication we retain the smallest support carrying at least $1-\eta$ of the row mass and renormalize. Discarding mass $\eta$ changes the row by total variation $\eta$ and reverse KL $-\log(1-\eta)$, making $\eta$ an explicit approximation tolerance rather than an arbitrary width. The radius study compares $\mathcal R=\{1\}$, $\{1,2\}$, and $\{1,2,4\}$ while fixing the support budget and the total graph-group weight; $\mathcal R=\{1\}$ is the paper default.

\subsection{Sparse Relation Matching}
\label{sec:candidates}

Full-corpus student scoring would defeat the purpose of sparse supervision. At a fixed graph-support budget, GGPKD deterministically retains the top-$K$ entries of the configured graph target and adds hard and uniform negatives. Top-$K$ directly minimizes omitted cached teacher mass for that budget; teacher-proportional and uniform support sampling are evaluated as controls rather than bundled into the method. Let $\Omega_i$ be the resulting candidate set. Sets are pooled and deduplicated across a batch $B$,
\begin{equation}
\mathcal P_B=\bigcup_{i\in B}\Omega_i,
\end{equation}
so every distinct candidate text is encoded once even when several anchors select it.

The resulting graph loss is a sparse objective, not an unbiased Monte Carlo estimator of the full-corpus softmax: both teacher and student distributions are renormalized on the selected support. Its coverage is therefore reported explicitly as the original teacher mass retained by the selected entries, including residual mass discarded when the graph artifact was truncated. The ambient target complements this graph objective by assigning teacher affinity across every scored candidate, including hard and uniform negatives outside the selected graph support.

All supervision in GGPKD takes a single form: a teacher-derived relation vector is restricted to a realized support and matched by a student softmax over that same support. For a center node $v$ with student similarities $c^S_v$, a nonnegative target $g$, a support $\Omega$ not containing $v$, and a temperature $\tau$, define
\begin{equation}
\mathcal D_v(g,\Omega,\tau)
=D_{\mathrm{KL}}\!\left(\operatorname{Norm}_\Omega(g)\,\middle\|\,
\sigma_\Omega\!\bigl(c^S_v/\tau\bigr)\right),
\qquad
\sigma_\Omega(z)_j=\frac{\exp(z_j)}{\sum_{u\in\Omega}\exp(z_u)}.
\label{eq:local-matching}
\end{equation}
Each loss below is an instance of $\mathcal D_v$; scales differ only in which target, support, and temperature they supply. A diffusion scale $r\in\mathcal R$ matches the cached target $g^T_{i,r}$ on the anchor's own support $\Omega_{i,r}=\Omega_i$ at temperature $\tau_{i,r}=\sqrt r\,\tau_i$: the ladder evaluates broader targets at a correspondingly coarser resolution and ties the one-hop student distribution to the bandwidth that generated its teacher row.

Restricted diffusion targets deliberately describe relations within $\Omega_i$, but they do not specify how an anchor should compare with candidates selected by other anchors. GGPKD therefore extends the family downward with an ambient scale $r=0$, whose target is the teacher's affinity row \emph{before} mutual-$k$NN sparsification. At a single shared bandwidth $\tau_0$,
\begin{equation}
g^T_{i,0}=\sigma_{\mathcal P_B\setminus\{i\}}\!\bigl(c^T_i/\tau_0\bigr),
\qquad
\Omega_{i,0}=\mathcal P_B\setminus\{i\},
\qquad
\tau_{i,0}=\tau_0.
\label{eq:ambient-scale}
\end{equation}
This is the same softmax-of-cosines construction as Equation~\eqref{eq:teacher-transition} with the sparsification removed: full support in place of $\mathcal M_k^T(i)$, one bandwidth in place of a per-row solution. The index $0$ marks the un-sparsified base of the family, not a zeroth operator power. Because the target is dense, every scored column receives its true teacher mass rather than an artifactual zero, and the same temperature appears on both sides of the KL.

The relational objective matches the whole family at once:
\begin{equation}
\mathcal L_{\mathrm{rel}}
=\frac1{|B|}\sum_{i\in B}
\sum_{r\in\{0\}\cup\mathcal R}
\omega_r\,\mathcal D_i\!\left(g^T_{i,r},\Omega_{i,r},\tau_{i,r}\right),
\qquad
\omega_0=\frac12,
\quad
\omega_r=\frac12\,
\frac{r^{-1}}{\sum_{q\in\mathcal R}q^{-1}}
\quad(r\in\mathcal R),
\label{eq:relational-loss}
\end{equation}
The ambient and graph groups therefore retain equal total weight as the radius set changes, while a graph scale's share within its group decays with diffusion time. Thus diffusion ranks the selected neighborhood at the requested resolutions, whereas the ambient scale calibrates each anchor against the broader shared pool.

\subsection{Amortization: Exposed-Support Row Reuse}

Equation~\eqref{eq:relational-loss} centers every local objective on a batch anchor. The shared pool contains many additional texts already encoded by the student, so GGPKD reuses teacher-selected candidates as auxiliary row centers. Let $\mathcal S_i\subseteq\Omega_i$ contain the candidates assigned positive mass by at least one realized diffusion target; hard and uniform negatives are excluded. The auxiliary centers are
\begin{equation}
\mathcal J_B
=\left(\bigcup_{i\in B}\mathcal S_i\right)\setminus B,
\end{equation}
where removing batch anchors avoids duplicating the one-hop term already present in $\mathcal L_{\mathrm{rel}}$. This construction introduces neither a traversal process nor a separate sampling distribution: $\mathcal J_B$ is a deterministic function of the realized candidate pool.

For $j\in\mathcal J_B$, only part of its transition row may be exposed in the current pool. Define that support and its teacher mass by
\begin{equation}
\Omega_j^{\mathrm{row}}
=\mathcal P_B\cap\mathcal M_k^T(j),
\qquad
m_B(j)=\sum_{\ell\in\Omega_j^{\mathrm{row}}}P_{j\ell}^T.
\end{equation}
Writing $\mathcal J_B^+=\{j\in\mathcal J_B:|\Omega_j^{\mathrm{row}}|\geq2\}$, the row objective is the one-hop instance of Equation~\eqref{eq:local-matching}, centered on non-anchor nodes and averaged uniformly:
\begin{equation}
\mathcal L_{\mathrm{row}}
=\frac{1}{|\mathcal J_B^+|}
\sum_{j\in\mathcal J_B^+}
\mathcal D_j\!\left(P_j^T,\Omega_j^{\mathrm{row}},\tau_j\right).
\label{eq:row-loss}
\end{equation}
Each row is matched at the bandwidth $\tau_j$ its target was built at, exactly as the $r=1$ scale is for anchors.
The exposed mass $m_B(j)$ records how much of the original transition row is visible but does not reweight the loss; teacher relevance has already influenced which centers enter $\mathcal J_B$. Rows with fewer than two exposed teacher neighbors are omitted because a conditional distribution on a singleton has identically zero KL. This term does not enlarge $\Omega_i$ for the original batch anchors. Its role is instead to amortize additional local objectives over non-anchor centers using embeddings already present in the update.

The complete objective is
\begin{equation}
\mathcal L_{\mathrm{GGPKD}}
=\mathcal L_{\mathrm{rel}}
+\lambda_{\mathrm{row}}\mathcal L_{\mathrm{row}}.
\label{eq:full-objective}
\end{equation}

\paragraph{Cost and deployment.}
Exact teacher-graph construction requires $O(N^2d)$ similarity work before sparsification, although approximate nearest-neighbor search can replace the all-pairs stage. The stored graph uses $O(Nk)$ edges. Candidate pooling avoids additional encoder passes for duplicated columns and auxiliary row centers; $\mathcal L_{\mathrm{row}}$ adds only their pairwise similarity and softmax computation, approximately $O(|\mathcal J_B||\mathcal P_B|d)$ per batch. The cached graph and teacher are used only during distillation, so inference uses the student encoder alone.
